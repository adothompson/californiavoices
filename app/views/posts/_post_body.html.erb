<%
  edit_loading_id = "spinner-#{post.id}"
  stars = post.ratings.size
  @story ||= post.discussion.story # in case @story is not already set
  -%>

<div class='negativespace'><%= post.body_html %></div>
